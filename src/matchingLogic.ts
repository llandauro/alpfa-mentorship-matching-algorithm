// Define ALPFA member that would be a mentee or mentor
export interface Member {
    name: string;
    experience: number; // in years
    field: string;
    careerStage: string; // e.g., "early", "mid", "late"
    studies : string; // e.g., "Computer Science", "Business", etc.
    objectives: string; // e.g., "career growth", "networking", etc.
}

